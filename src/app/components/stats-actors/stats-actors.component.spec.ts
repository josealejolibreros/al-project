/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { StatsActorsComponent } from './stats-actors.component';

describe('StatsActorsComponent', () => {
  let component: StatsActorsComponent;
  let fixture: ComponentFixture<StatsActorsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ StatsActorsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(StatsActorsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/